import { Base, DomRenderer, Type } from "./type";
export declare class DVSR extends Base {
    static $t: Type;
    private a;
    container: DomRenderer;
    constructor(a: DomRenderer);
    getString(resourceName_: string): string;
}
