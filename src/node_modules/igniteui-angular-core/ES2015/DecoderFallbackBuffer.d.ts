import { Base, Type } from "./type";
export declare abstract class DecoderFallbackBuffer extends Base {
    static $t: Type;
    constructor();
    readonly abstract d: number;
    abstract a(a: number[], b: number): boolean;
    abstract c(): string;
    abstract b(): boolean;
    e(): void;
}
